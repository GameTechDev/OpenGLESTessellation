diffuse = texture( name="Texture0", uv=uv0 * (1,1))
normal = texture( name="Texture1", uv=uv0 * (1,1))
specularTmp = texture( name="Texture2", uv=uv0 * (1,1))
specularityTmp = texture( name="Texture3", uv=uv0 * (1,1))
specular = (specularTmp * specularityTmp) * 7
specularity = 5
ambient = diffuse